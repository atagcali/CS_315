program {

	// this program checks function definition , 
	// assignment and declaration statements
	// (add - minus) operation statements
	// function call , while loop , do-while loop 
	// primitive function call , parameter 

	int addfunc ( int a , int b ) {
		
		int x
		x = a + b
	return x;
	}
	addfunc(6 , 12)

	int i = 10;
	while ( i > 5){
		addfunc(10 , 15)
		i -= 1
	}


	int projectMember = 3
	string fullName;
	string name
	string surname
	name = "Ahmet"
	surname = "Baykal"
	fullName = name + surname
	
	do {
	
	fullName = name + surname

	name = "alp"
	surname = "agcali"

	projectMember -= 1

	string newFunc(char a)
	
	name = name + a
	fullname = name + surname
	} while(projectMember > 1)

	readData(SENSOR5)
	readData(SWITCH_5)
	timeStamp()

	sendFunc()

	rcvFunc()

	connectFunc()


	
}
